import {Injectable} from '@angular/core';
@Injectable(

)
export class Test {
  getMessage() {
return 'hello';
  }
}

// if injectable set the name of the injactable class name in providers of component
