import { AppnotDirective } from './appnot.directive';

describe('AppnotDirective', () => {
  it('should create an instance', () => {
    const directive = new AppnotDirective();
    expect(directive).toBeTruthy();
  });
});
